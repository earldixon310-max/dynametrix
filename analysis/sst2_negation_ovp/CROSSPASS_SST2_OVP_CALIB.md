# Cross-Pass Record — SST2_OVP_CALIB (cut-point calibration sub-study, lock 1)

Tracks the two-pass review + lock progress for `PRE_REGISTRATION_SST2_OVP_CALIB.md`, per spec §7 and the §7 two-pass interpretation in `OVP_DESIGN_HISTORY.md`. The pre-reg does not narrate its own review-progress; this file holds the mutable status.

**Lock bar:** two independent passes return **no lock-blocker on byte-identical artifacts**; a blocker resets the count; non-blockers queue without folding; the fix-author cannot clear.

## Current status: **EXECUTED — outcome MIS-SPECIFIED.** Locked at `sst2-ovp-calib-lock` (2/2 clean cold passes on byte-identical revision-3 artifacts; operator §11 sign-off implicit in the lock execution); single run completed under seed `0x55712`; outputs at `sst2-ovp-calib-result`. **Verdict: MIS-SPECIFIED via §6 check 1 (`τ_lo = 0.0`, strict positivity failed; checks 2–3 passed)** — the pre-committed exit; lock 2 does not proceed on this calibration. Outcome document: `RESULT_SST2_OVP_CALIB.md`. **Open gate: cold cross-pass on the result document** (spec §7 citation gate, per the OVP_POSCONTROL_v1 precedent). All v0.x non-blockers below remain queued.

## Revision-3 cold pass 2 (external): NO LOCK-BLOCKER — bar met
Independent fresh reader (no design context). All ten checks passed, with check F run **mechanically in both directions**: pinned→implemented (every §7 item confirmed in the output set, incl. null means, durable per-example hash in file + meta, canonical+used seed/reps) and computed→pinned (every results-JSON quantity traced to §7; no unpinned statistic). Also independently verified: the §3 unpaired-splits seed mechanism against the code (no cross-construction contrast computed, marginal distributions only); §6 check 3 implemented verbatim (non-strict ≤ monotonicity over 8 points, nulls mean ≤ 1e-9, failure direction conservative); the §5↔§6 mutual consistency of the clearing rule and the band gap; scope (USABLE BAND / MIS-SPECIFIED are not ledger verdict categories). **Corroboration note:** pass 1's closest call — the top-level `SEPARABLE` boolean vs the "nothing beyond the pinned set" clause — was independently examined and judged within the pin ("falls within 'check booleans and the verdict string'"). **VERDICT: NO LOCK-BLOCKER.**

### Combined v0.x queue (both revision-3 passes; queued un-folded — fold only at a future forced revision)
From pass 1: (1) §7 nesting wording (`hdg_distributions` is metric-first); (2) name `SEPARABLE` verbatim in §7; (3) explicit `method="linear"` arg in `pctl`; (4) §2 cross-hardware reproducibility over-claim; (5) "draws its noise" vacuous for null-redundant; (6) hash-file parse format assumption.
From pass 2 (overlapping items merged): (7) §2 sentence deferring inference mechanics (batch 64, max_length 128, truncation) to the locked script; (8) §3 one-line note that construction evaluation order (RNG stream consumption) is pinned by the script's `cons` list; (9) `--out` lacks a non-canonical warning; (10) per-example CSV carries pass-through input columns beyond the four named quantities (audit-helpful; harmless); (11) lock manifest should state the canonical run directory.

## Revision-3 cold pass 1 (external): NO LOCK-BLOCKER
Independent fresh reader (no design context). All checks passed, including **check F as a mechanical enumeration**: every §7 pinned output confirmed produced (full AUC/AP per-replication arrays, null P95s + means, meaningful P5s/means, AP panel means, the pinned baseline diagnostic, check booleans + verdict, full meta incl. durable per-example hash), no result-affecting unpinned computation found, and the §3 unpaired-splits seed mechanism confirmed matching the code. AP relabel verified on both terms and non-gating; provenance, band/MIS-SPECIFIED logic, grid/seed parity, lock discipline (used-vs-canonical recorded for seed and reps), scope, and pinning all conformant. **VERDICT: NO LOCK-BLOCKER.**

### v0.x queue from revision-3 pass 1 (queued un-folded to preserve byte-identity for pass 2)
1. §7 nesting wording: `hdg_distributions` nests `{AUC: {…}, AP: {…}}`; §7 reads as if construction names are top-level. Content identical; one-line clarification.
2. Top-level `SEPARABLE` boolean not literally named in §7's pinned set — reader's closest call, judged non-blocking because it is a deterministic conjunction of the three pinned check booleans, bijective with the pinned verdict string (zero new information / post-hoc freedom). Name it in §7 at next forced revision (or drop the key).
3. `pctl` relies on numpy's default `'linear'` rather than an explicit `method="linear"` argument — identical today; explicit arg hardens against environment drift.
4. §2 "deterministic… reproducible" mildly over-claims across hardware (float nondeterminism; batch-64/padding pinned only via the locked script) — harmless under single-execution + hashed materialization.
5. §3 "each construction draws its noise" is vacuously true for null-redundant (zero draws) — wording.
6. Hash-file parse (`.split()[0]`) assumes `hash [filename]` format — robustness nit.

## Fresh cold pass 2 (external, revision-2 bytes): LOCK-BLOCKER FOUND (×3) — count reset
Independent fresh reader; statistical core passed every check (HDG form, provenance, band/MIS-SPECIFIED logic, AP relabel both terms + non-gating, grid/seed parity, single-execution, scope, pinning). All three blockers were **check-F (output/pin conformance) catches**:
- **B1 — pinned-but-unimplemented output:** §7/§8 promised "the HDG distributions" written to the results file; the script persisted only scalar summaries. The null **means** deciding §6 check 3 appeared nowhere in the output — under single-execution discipline these are unrecoverable without a forbidden re-run, so check 3 could not be audited from the frozen artifact.
- **B2 — unpinned-but-computed output:** `baseline_auc_median_nullnoise_splits` frozen into results but pinned nowhere — exactly the reporting degree of freedom the pin discipline exists to close.
- **B3 — promised mechanism not durable:** the per-example CSV SHA-256 (§2, inherited by lock 2 per §7) lived only in transient stdout.
Plus six non-blockers (seed-derivation wording, silent `--reps`, `≤ ~0` tolerance, non-strict monotonicity wording, revision-identity check at lock, relative-path fragility).
**Significance noted for the design history:** (a) pass 1 and pass 2 read byte-identical artifacts and diverged (clean vs 3 blockers) — the two-pass bar catching what a single clean pass missed; (b) this was the **first deployment of the explicit output/pin-conformance check (F)**, queued for AEPF v1.1 after the AP/bootstrap catches — it caught three more divergences on its first outing.

## Pre-lock revision 3 — persistence contract; bytes changed, count reset (2026-06-07)
**B1 fix (direction: more persistence, not weaker promises):** `calibrate()` now persists the **full per-replication HDG arrays** (AUC and AP, all R_cal values per construction, under `hdg_distributions`) and the **null means** (`support.null_mean_AUC` — the §6-check-3 inputs). §7 rewritten as an explicit **persistence contract** pinning the exact output set ("everything needed to audit without a re-run, and nothing beyond this pinned set"); §8 step 3 aligned.
**B2 fix:** `baseline_auc_median_nullnoise_splits` now **pinned in §7** (context diagnostic, non-gating) rather than deleted — it has audit value.
**B3 fix:** the run writes `sst2_per_example_sha256.txt` and records the hash in the results meta; §2/§7 updated ("never stdout-only").
**Folded non-blockers (both passes, free under the forced reset):** §3 seed-derivation wording now states the actual mechanism (one child per replication; constructions draw split seeds from the advancing stream; **not split-paired**, sound because only marginal distributions are used; lock 2 must pin its split structure explicitly); `--reps` now warns when non-canonical and meta records canonical+used for both seed and reps; §6 check 3 states the `1e-9` tolerance and **non-strict** monotonicity; stale `MODEL_REVISION` header comment fixed; docstring notes the run-from-study-dir requirement; `build_manifest_sst2_calib.py` now **aborts the lock unless audit `model_revision.txt` == manifest pin == script pin** (3-way identity).
**Validation:** both scripts `py_compile`-clean; dataset hash + 3-way revision identity verified against the real files; the new output structure JSON-round-tripped (10 constructions × 200 values, numpy→list conversions correct). Calibration math (grid, AP, cut-point rules) untouched — the revision-1 numerical smoke remains valid. **No design blocker.**

## Fresh cold pass 1 (external, revision-2 bytes) — SUPERSEDED by revision 3 (pass 2's blockers reset the count): NO LOCK-BLOCKER
Independent fresh reader (no design context; pre-reg + script + spec body + lock notice). Verified all ten gating checks: HDG form (= spec §1), cut-point provenance (nulls→τ_lo, meaningful→τ_hi; negation absent; the spec §1 bootstrap path), strict band + δ-gap + genuine pre-committed MIS-SPECIFIED exit, **AP error-class relabel correct on BOTH terms and non-gating** (τ's read `hdg_auc` only), grid/percentile/seed parity between pre-reg and script, **resampling = single stratified-splits scheme with no bootstrap pinned anywhere as an output** (the header `§1 "bootstrap path"` correctly read as the provenance sense), output/pin/mechanism conformance, single-execution + lock discipline, scope disclaimers, and model/data pinning incl. the input-hash guard. Noted (correctly) that `null_redundant = 2B−1` is a monotone affine of B so its HDG is exactly 0 every replication (harmless, slightly strengthens τ_lo), and that the MODEL_REVISION ↔ audit-manifest SHA equality is external to these four artifacts (a concrete SHA is pinned, which is the bar). **VERDICT: NO LOCK-BLOCKER.**

### Pass-1 non-blockers (originally queued un-folded to preserve byte-identity; **all now folded into revision 3** under the forced reset)
1. Stale `MODEL_REVISION` header comment still says "must be set … before lock" though a concrete SHA is set — wording lag, no run effect.
2. `meta.R_cal` records `args.reps` rather than the `R_CAL` constant; equal under the canonical run, only a `--reps` smoke (non-canonical) would mislabel.
3. Pre-reg §6.3 `"≤ ~0"` vs code `≤ 1e-9` nulls-nonpositive tolerance — faithful encoding; only risk is a conservative (pre-committed) MIS-SPECIFIED. One clarifying word in a future revision.
4. **(design-substantive; carry to lock 2)** Arms within a replication do not share an identical split (shared advancing `rng`; `construct()` consumes draws before `hdg()`). The pre-reg never requires paired splits and each arm gets `R_cal` valid master-seeded stratified splits, so calibration soundness is unaffected — but **lock 2's candidate pre-reg should pin paired-vs-unpaired splits explicitly** so the negation comparison's split structure is a stated choice, not an emergent one.
5. `baseline_auc_median_nullnoise_splits` sampled over null-noise splits only — construction-independent, already documented; reporting detail.

## Bundled pre-lock revision — bytes changed, count reset (2026-06-07)
Two fixes applied in one revision cycle (the byte-window was already open, so folding now beats queueing — no extra reset cost):

1. **σ_m grid (operator Option 2):** refined from {0.5, 1.0, 1.5, 2.0, 3.0, 4.0} to **{0.5, 1.0, 1.5, 2.0, 2.5, 3.0, 3.5, 4.0}** (uniform 0.5 spacing) — fixes the coarse high-σ_m resolution where the `τ_hi` "weakest-clearing" rule operates; converts the previously-queued grid-dependency non-blocker into a pre-lock fix. Pre-reg §4/§5/§6/§11.4 + script `SIGMA_M_GRID` updated.
2. **AP error-class fix (operator Option A):** the cold-pass-2 "informal labeling" non-blocker was, on code review, a real intent-vs-implementation mismatch — §3 says AP targets the rare error class, but the code computed AP on the `y=1` majority (correct) class. AP is class-asymmetric, so the panel did not serve its stated imbalance-robustness purpose. Fixed `hdg()` to treat error as positive (`average_precision_score(1−yte, 1−p)`); §3 gains a pinned implementation sentence; §6/§11.1 updated. AP remains **non-gating** (cut points AUC-only). The cold-pass-2 "new version → new pre-registration" wording fix was folded into the same §3 edit.

**Re-smoke (8-pt grid + corrected AP):** strictly monotone over 8 points; USABLE BAND survived and was **unchanged by the AP fix** (AP non-gating confirmed in code); AP panel now nulls-nonpositive / meaningful-positive-decreasing, with rare-class amplification (strongest meaningful ΔAP +0.478 > ΔAUC +0.295). No design blocker — see `SCRIPT_BUILD_FINDINGS_SST2_CALIB.md`.

Per the lock bar, **a change to the locked artifacts resets the cold-pass count to zero.** The earlier two cold passes were on the superseded (6-point grid, majority-class-AP) artifacts and do not carry over.

## Fresh cold pass 1 (external, on the grid+AP "revision 1" bytes) — SUPERSEDED by revision 2: no lock-blocker on the gating path
Independent fresh reader (no design context; given pre-reg + script + spec body + lock notice) verified all eight gating checks on the refined bytes: HDG form (= spec §1), external cut-point provenance (nulls→τ_lo, meaningful→τ_hi, negation absent), strict band + δ-gap + genuine MIS-SPECIFIED exit, the **AP error-class relabel confirmed in BOTH terms** (candidate and baseline) with AP confirmed non-gating (τ's from AUC arrays only), the 8-point grid/percentile/seed pins matching between pre-reg and script, single-execution discipline, scope disclaimers, and model/data pinning (concrete `MODEL_REVISION` SHA, deterministic inference). **VERDICT: NO LOCK-BLOCKER.**
**But** the reader's non-blocker #1/#4 surfaced a real pre-reg/script inconsistency, with its own escalation note: the **bootstrap sensitivity panel was pinned in pre-reg §3/§11 (and the script docstring) but implemented nowhere**, and §7's output list omitted it — *"if the lock authors regard the bootstrap panel as a required output, this rises to a blocker, since a pinned, signed-off output would be missing from the single locked run."* Operator (Earl) agreed and **elevated it to must-fix-before-lock** on byte-discipline grounds (locking a pre-reg that pins an unproduced output describes a run that didn't happen). Resolving it changes bytes → this otherwise-clean pass is **superseded**, not credited.
Other reader non-blockers (folded into revision 2, below): in-script input-hash assert (#2); `baseline_auc_median` field-name clarity (#3).

## Bundled pre-lock revision 2 — bootstrap struck + folds; bytes changed, count reset (2026-06-07)
Operator chose **Option A (strike the bootstrap)** over Option B (implement it). Rationale: the bootstrap mechanics were never pinned (OOB vs resample-then-split vs resample-the-metric, stratified vs not — each a different estimand), so implementing meant designing + pinning + cold-reviewing genuinely new work for a non-gating panel partly covered by the corrected AP. Strike makes the locked pair consistent with the partition-variance estimand the study actually targets.
- **Bootstrap struck** from pre-reg §3, §11 pin 2, and the script docstring (§7 already omitted it). §3 retains a neutral estimand note: partition-variance is the target; a population-estimand bootstrap is *deliberately not run in this version* (a scope decision, not an inability), and would be a new pre-registration with its own pinned mechanics. (The spec-terminology phrase `§1 "bootstrap path"` in the header is a different sense — the external-calibration provenance path — and stays.)
- **Folded #2:** `materialize_per_example()` now asserts the input CSV sha256 against `sst2_validation_sha256.txt` and aborts on mismatch (fail-fast even outside the lock harness; complements the manifest's abort). Verified PASS against the real pinned data.
- **Folded #3:** `baseline_auc_median` → `baseline_auc_median_nullnoise_splits` with an inline note (baseline AUC is construction-independent; sampled over null-noise splits).
- **Smoke:** edits are doc/guard/rename only — the calibration logic is numerically unchanged (bootstrap was never in code), so the revision-1 AP re-smoke remains the valid logic validation. Script `py_compile`-clean; the new input-hash guard exercised against the real files (expected == actual → PASS).
Per the lock bar, this resets the cold-pass count to zero on the revision-2 bytes.

## Pre-cold-pass steps (complete)
- **Warm pass** (reviewer had design context — *not* one of the two cold passes): clean; the three pre-commitments (stratified-splits-primary + named estimand, mis-specification exit, AUC-primary/AP-panel) confirmed correctly incorporated. NB1–NB3 folded *before* the cold passes (free at that stage).
- **Build-and-smoke** (`calibrate_sst2_cutpoints.py`, `SCRIPT_BUILD_FINDINGS_SST2_CALIB.md`): built strictly to the pre-reg; logic smoke-validated on simulated `(B, y)` — **USABLE BAND survived** the ~91% imbalance, no design blocker. `MODEL_REVISION` is a fill-at-lock item.

## Cold pass 1 (external) — SUPERSEDED (on the 6-point grid; does not count toward the refined-bytes bar): no lock-blocker
Confirmed conformance: HDG measure form (§3 = spec §1), cut-point provenance/bootstrap (external calibration, no self-sourcing; negation not computed here so the ruler isn't built from what it measures), strict band with the `τ_hi`-rule auto-satisfying `≥ δ` or exiting MIS-SPECIFIED, single-execution + lock bundle + cross-pass discipline, and scope disclaimers conformant (yields no ledger verdict, so does not move the §6 ladder). Flags — **all non-blocking and already disclosed in the artifact**: (1) AP panel fenced from band determination — *now also confirmed in code:* `τ_lo`/`τ_hi` use the AUC HDG arrays only, AP only in the reported panel; (2) the meaningful construction's intentional, disclosed target leak (legitimate for a calibration ruler); (3) `τ_hi` grid-dependency documented as a limitation; (4) §2 Ancestry/baseline-simplicity rule correctly belongs to lock 2, not here. Independence: fresh reader, no design context, given pre-reg + spec body + lock notice.

## Cold pass 2 (external) — SUPERSEDED (on the 6-point grid; does not count toward the refined-bytes bar): no lock-blocker
Independently confirmed conformance (provenance/bootstrap, strict band + δ-gap, one-measure with AP fenced, AUC orientation, scope discipline). Two non-blockers, both editorial/non-gating, **queued for v0.x (not folded — preserving byte-identity):** (1) §3 "AP-primary would be a new version" *overstates* — per spec §1 the metric is a per-study instantiation, so AP-primary is a new *pre-registration*, not a new OVP *version* (§6 already states this correctly); fix §3 wording in v0.x. (2) §3 calls AP's focus the "rare-positive (error) class," but the error class is coded `y = 0`; informal labeling, AP non-gating, no effect on cut points. Independence: fresh reader, no design context, given pre-reg + spec body + lock notice.

**Two-pass result on the 6-point/majority-AP artifacts: SUPERSEDED.** Both passes returned no lock-blocker, but on the now-superseded bytes; the bundled revision (above) resets the count. The two editorial non-blockers each cold pass surfaced have been **folded into the revision** (σ_m grid resolution; AP "new version" wording), and the AP-class flag was upgraded from "informal labeling" to a fixed implementation mismatch. Nothing remains queued for v0.x from this study.

## Remaining gates (on the refined bytes)
- ~~`MODEL_REVISION` fill~~ **DONE:** `714eb0fa89d2f80546fda750413ed43d93601a13`, triple-cross-checked. (F1 resolved.)
- ~~Lock-bundle manifest generator~~ **DONE:** `build_manifest_sst2_calib.py` (fingerprints external dataset + model; aborts the lock on a dataset-hash mismatch).
- ~~Bundled pre-lock fixes~~ **DONE:** revision 1 (σ_m grid + AP error-class + wording), revision 2 (bootstrap struck + input-hash assert + field rename), revision 3 (persistence contract B1–B3 + all queued non-blockers folded); validated each cycle, no design blocker.
- ~~Two cold passes~~ **DONE:** 2 of 2 clean on the revision-3 byte-identical artifacts.
- ~~§11 sign-off + lock + single run~~ **DONE (2026-06-08):** manifest built clean (dataset hash + 3-way revision identity verified at lock); `sst2-ovp-calib-lock` signed; single run executed (n=872, 78 errors, accuracy 0.911); outputs committed at `sst2-ovp-calib-result`.
- **Cold cross-pass on `RESULT_SST2_OVP_CALIB.md`** (citation gate) — the open gate. **Progress:**
  - **Result review pass 1: BLOCKER FOUND (×4).** Verdict derivation confirmed sound (mechanical from locked §5/§6; MIS-SPECIFIED stands); all four blockers were **reporting-fidelity defects in the result document**, each independently re-verified against the frozen data and **fixed**: (B1) "200/200 positive" for σ_m=0.5 was false — actual **199/200** (one replication at −0.0070; the drafting error was a `%.0f` format rounding 99.5%→"100%"); (B2) dataset-hash suffix transcribed wrong (`…6588376d` → **`…2588376d`**); (B3) AP noise P95 misreported as −0.038 via a naive order-statistic — under the pinned numpy `'linear'` rule it is **−0.0399** (sign/contra-indication unaffected); (B4) the confidence-compression figures were credited to the results JSON but derive from `sst2_per_example.csv` — re-cited to the correct artifact (hash in meta) with the header claim narrowed, and recomputed under the proper percentile rule (IQR [0.9968, 0.9998], P5 0.9146, 84.4% > 0.99). Non-blockers folded: P5 rounding consistency (0.01613), 8-char hash suffixes, and a lock-convention note explaining the locked pre-reg body's residual "DRAFT" line (tag-only/byte-exact, as with the spec).
  - **Lesson (reporting layer):** result documents inherit the same pinned-rule discipline as analysis code — every reported statistic must be computed by the pinned procedure (not an eyeballed order statistic), and counts must never pass through lossy formatting. Both B1 and B3 originated in the drafter's quick-look computations, not in the frozen data.
  - **Count reset; route two fresh result-review passes** on the corrected document.
- Then: the continuation decision (feature decompression / floor-rule revision / larger substrate / abandon) — each a **new pre-registration**; lock 2 remains blocked on a valid calibration.
