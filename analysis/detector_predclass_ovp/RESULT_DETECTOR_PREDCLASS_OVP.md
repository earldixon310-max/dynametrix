# RESULT — DETECTOR_PREDCLASS_OVP (candidate study; OVP real candidate #4) — first under the Descriptor Justification Layer

**Study:** DETECTOR_PREDCLASS_OVP — does **`pred`** (the detector's binary *predicted class*, 0=Human / 1=ChatGPT) add Held-out Discriminative Gain beyond the *folded* baseline **`confidence`** (`max(p,1−p)`) in predicting ChatGPT-detector correctness? Fourth real candidate on the ChatGPT-detector substrate; the first candidate generated and governed under the OVP v0.2 **Descriptor Justification Layer**.
**Governing spec:** OVP v0.1 @ `ovp-v0.1-lock`.
**Lock:** signed tag `detector-predclass-ovp-lock` (commit `d4964f0`; pre-reg + `judge_predclass.py` + manifest; **warm + two clean cold passes (post-fold)** after the CP2-N1 foil-caveat fold and four CP1-post-fold consistency fixes).
**Execution:** single run, master seed `0xC1A55D`, `R = 200` paired stratified splits, foil permutation stream `SeedSequence(0xC1A55D ^ 0xF011)`, 2026-06-10 UTC; outputs at signed tag `detector-predclass-ovp-result`.
**Citation gate:** citable only after this document's own cold cross-pass (spec §7), whose brief includes the D-1 banner check (here: `foil_clears_tau_lo == False`, so the suspend-interpretation banner is correctly **absent**).

---

## Verdict: **INCONCLUSIVE**

| Quantity | Value |
|---|---|
| **`D` = median(HDG_AUC[1..200])** | **0.028142692244269796** |
| Inherited band `[τ_lo, τ_hi]` | `[0.024589, 0.068291]` (provenance `detector-ovp-calib-result`) |
| Band relation | `τ_lo ≤ D ≤ τ_hi` → **Inconclusive** (margin above `τ_lo`: **+0.00355**) |

`D` falls in the pre-registered closed band — **a borderline Inconclusive**, just above the noise floor (`D − τ_lo = +0.0036`; **43.5% of replications fall below `τ_lo`**, 0/200 reach `τ_hi`). Under the pinned *linear* estimator, the binary predicted-class direction adds only a small, ambiguous gain beyond folded confidence. Per spec §6 this is an abstention, recorded with parity — **no legitimacy claim** (§0.5.4 locked interpretation).

This is OVP's **fourth** real candidate verdict. Ledger: `truncated` Inconclusive, `text_length` Not-Validated, `predicted_prob_ai` Inconclusive, `pred` Inconclusive — **four honest non-positives, zero false positives** in four real audits; the operational rung (§6.2) holds.

## The numbers (auditable from `detector_predclass_results.json`, pinned numpy `'linear'` rule)

- **HDG_AUC distribution (200 paired splits):** P5 **+0.00662**, **median +0.02814** (`= D`), mean **+0.02656**, P95 **+0.04569**. **0/200 reach `τ_hi`**; **43.5% below `τ_lo`** (56.5% in band) → borderline.
- **Median ≈ mean** (+0.02814 vs +0.02656) — the `D = median` pin is not decision-relevant (mean also in band → Inconclusive).
- Error-class **AP** (non-gating): median **+0.00865**, small positive.
- n = 2000, errors = 644.

## The permuted-`pred` foil (non-gating) — band-validity confirmed; D-1 banner not required

| Foil quantity | Value |
|---|---|
| `D_foil` = median(foil HDG) | **−0.00508** (`foil_verdict` = Not-Validated) |
| foil HDG mean / P5 / P95 | −0.00594 / −0.03128 / +0.01246 |
| `foil_frac_below_tau_lo` | **1.000 (200/200)** |
| `foil_pre_commitment_met` | **True** |
| `foil_clears_tau_lo` | **False** |

The permuted, **target-independent** `pred` was rejected by the band in **every one of 200 replications** (median ≈ 0, below `τ_lo`). This is exactly the pre-committed expectation (§6): a feature carrying only the 67/33 marginal frequency, with `pred↔correctness` destroyed, cannot lift held-out AUC — and the band correctly rejects it. The foil therefore **validates the band/method**, and because `foil_clears_tau_lo == False`, **no methodological red-flag banner is required** (D-1 obligation satisfied; the run is methodologically trustworthy).

## Interpretation — the intercept asymmetry is real, but folded confidence already largely encodes it

The non-gating **per-predicted-class diagnostic** (computed once in the locked run; the disclosed mechanism) reproduces the large class-asymmetry that generated this candidate:

| predicted class | accuracy | n |
|---|---|---|
| predicts **AI** (`pred=1`) | **0.773** | 652 |
| predicts **Human** (`pred=0`) | **0.632** | 1348 |
| **gap** | **+0.141** (14.1 points) | — |

This candidate was generated by the ledger itself: DETECTOR_DIRECTION_OVP (#3) surfaced this 14.1-pt intercept asymmetry and gave `[B,pred]` (the clean intercept lever the slope-coupled `[B,p]` could not represent) as the natural follow-up. The **pre-registered genuine uncertainty** (§0.5.3) was never whether the asymmetry exists — it does — but **whether folded confidence already encodes it**. The run answers: **largely yes.** Even with the clean intercept lever, `pred` lands a borderline Inconclusive — folded confidence (lower when the detector predicts Human, the class it is worse at) already tracks most of the reliability asymmetry, so `pred`'s incremental contribution beyond confidence sits in the ambiguous band.

**A pre-registered speculation, honestly not borne out.** #3's result doc recorded that a `[B,pred]`-shaped study "would plausibly **Validate**." This run — that exact study — returns **Inconclusive**, not Validated. The speculation was a legitimately-recorded, separate question; the answer is that folded confidence is closer to a sufficient statistic for the asymmetry than the bare marginal gap suggested. This is the pre-commitment discipline working as designed: the 14-pt marginal gap did **not** predetermine the verdict (item-3's tempering of the original "likeliest first Validated" framing is vindicated on the merits), and an over-optimistic forecast was caught by the locked run rather than carried forward.

Per §5 the scalar `D` is not cross-row comparable; the verdict-level observation is only that **both** the slope-axis candidate (#3) and the intercept-lever candidate (#4) land borderline Inconclusive — the clean intercept lever did not change the outcome.

**Estimator-conditional (spec §0/§9).** The verdict is conditional on the pinned linear estimator and the folded-vs-predicted-class contrast. Inconclusive is an abstention, not a Not-Validated: this run does not establish that `pred` adds *nothing* — only that its incremental contribution beyond confidence is unresolved at this band.

## What this establishes / does not

- **Does:** issue OVP's fourth real verdict (operational rung holds, zero false positives); establish that the detector's large intercept-level class-asymmetry, given the clean `[B,pred]` intercept lever under the linear estimator, does **not** clearly add beyond folded confidence (borderline Inconclusive) — folded confidence already largely encodes it; confirm via the unanimous foil (200/200 below `τ_lo`) that the band correctly rejects a target-independent feature; demonstrate the Descriptor Justification Layer end-to-end on first prospective use (mechanism disclosure, dual locked interpretation honored, surface foil operative and informative).
- **Does not:** decide scientific interest/use; establish Not-Validated (the verdict is Inconclusive, not below `τ_lo`); separate redundant-vs-noise (v0.2); claim anything about a nonlinear estimator; reach community-validated (§6.3 needs an externally-authored candidate).

## Ledger row (spec §5)

| field | value |
|---|---|
| candidate | `pred` (binary predicted class, 0=Human / 1=ChatGPT) |
| baseline | `confidence` (folded `max(p,1−p)`) |
| substrate | ChatGPT-detector RoBERTa (`d2b342c6…`) over RAID test subsample (`a29f8f2c…`), n=2000 |
| measure `D` | median HDG via AUC; standardized L2 logistic; stratified 50/50 paired × 200; seed `0xC1A55D` |
| cut points | `τ_lo=0.024589`, `τ_hi=0.068291`; provenance `detector-ovp-calib-result` |
| **verdict** | **Inconclusive** (`D=0.028143 ∈ [τ_lo, τ_hi]`, borderline; +0.0036 above `τ_lo`) |
| foil (non-gating) | permuted-`pred` `D_foil=−0.00508`, 200/200 below `τ_lo` → band validated; no red-flag |
| cross-pass | warm clean; 2/2 cold clean (post-fold), CP2-N1 fold + 4 CP1-post-fold fixes applied (`CROSSPASS_DETECTOR_PREDCLASS_OVP.md`) |

## Provenance

`B,y,pred` inherited byte-identical from the calibration-locked `detector_per_example.csv` sha `24dac078…01afc643` (hash-verified; **no materialization, no model run**); cut points asserted byte-identical to `detector_calibration_results.json` at runtime (`verify_cut_points`) and lock (manifest); model `Hello-SimpleAI/chatgpt-detector-roberta` @ `d2b342c6…`; dataset `a29f8f2c…`; estimator `StandardScaler(train-fit) → LogisticRegression(lbfgs, C=1.0, max_iter=1000, fit_intercept=True)`; seed `0xC1A55D` (= canonical), R = 200 (= canonical); foil permutation stream `SeedSequence(0xC1A55D ^ 0xF011)` (independent of the split stream). Full per-replication HDG distribution (real AUC + error-class AP + foil AUC, 200 each) persisted under `hdg_distribution`; environment per `materialization_manifest_detector_predclass.json` (Python 3.12.10, numpy 2.1.2, scikit-learn 1.8.0).
