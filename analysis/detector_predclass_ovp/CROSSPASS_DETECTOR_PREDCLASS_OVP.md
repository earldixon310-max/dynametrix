# Cross-pass record — DETECTOR_PREDCLASS_OVP (OVP real candidate #4)

Governing: OVP v0.1 @ `ovp-v0.1-lock`. First study under the v0.2 Descriptor Justification Layer.
Sequence (spec §; pre-reg §8/§10): **build-and-smoke → warm pass → two cold passes (≥1 cold, fix-author cannot clear) → lock → single run → result + result cold-pass.**

---

## Build-and-smoke — COMPLETE
- `py_compile` clean: `judge_predclass.py`, `build_manifest_detector_predclass.py`.
- Synthetic numpy smoke mirror (`smoke_synthetic_predclass.py`; sandbox has no sklearn — byte-identical sklearn smoke to be run on local Python): **PASS**.
  - meaningful binary candidate → D=0.07553 (adds gain); **its permutation (foil) → −0.00046 (collapses)**.
  - null binary → D=−0.00127 (~0); its foil → −0.00100 (~0).
  - Validates: loop machinery; shared-split + independent foil-permutation-stream plumbing; **foil mechanic destroys a real binary's gain while leaving a null unchanged**.
  - Real `pred` NOT loaded (no-peeking preserved; smoke is synthetic-only).
- Output-conformance (check F) by inspection: persisted keys match pre-reg §7 exactly; nothing beyond the pinned set.

## Warm pass — COMPLETE (Earl), NO BLOCKERS
- Walked spec conformance A–K: HDG form (§1), mechanistic Ancestry (§2), verdict rule (§6), scalar `D = median` no-trim, cut-point provenance + runtime assert, paired-split + shared-baseline foil construction, no-peeking/single-execution, output conformance (§7), scope, all six Descriptor-Justification items, foil pre-commitment. All conform.
- Noted as the cleanest pre-reg/script pair the arc has produced; #1–#3 hardening lessons baked in from draft 1; Descriptor Justification Layer's first contact needed no revision to the Layer itself.
- **One borderline editorial item (non-blocker), FOLDED before cold routing:** §7 said "foil band fractions" (plural) while the script computed only `foil_frac_below_tau_lo`. Fix applied — script now emits all three foil band fractions (`foil_frac_above_tau_hi` / `foil_frac_below_tau_lo` / `foil_frac_in_band`), symmetric with the real candidate's three; §7 phrasing tightened to name all three explicitly. Foil distribution now as auditable as the real candidate's; ambiguity removed.
- Warm pass is context-saturated (design conversation) — NOT a cold pass. Two genuinely cold readers required per the codified bar.
- Post-fold re-check: file-tool view authoritative — foil block lines 141–143 (real) ↔ 151–155 (foil) symmetric; tail intact (bash mount-truncation artifact disregarded, local `py_compile` is authoritative). Synthetic smoke unaffected by reporting-only change (still PASS).

## Cold pass 1 — COMPLETE (genuinely cold reader; no design history). CONDITIONAL CLEAR → fix applied.
Packet: spec body, pre-reg, `judge_predclass.py`, `build_manifest_detector_predclass.py`, inherited `detector_calibration_results.json`. Reader did NOT execute the judge (running it computes the sealed HDG; a pre-lock cold reader computing `D` would break the seal) — verified read-mechanism + disclosed quantities only; `D` stays sealed. Correct discipline.
- **Charge 1 (v0.1 gating) — PASS (logic; value sealed):** §1 HDG orientation correct (candidate−baseline AUC, higher-is-better; τ_lo>0 guarantee holds); §2 all five incl. mechanistic Ancestry; §6 partition open/open/closed with boundaries → Inconclusive, `band_relation` exactly-one-fires; `D=median` all 200 no-trim.
- **Charge 2 (inheritance) — PASS:** runtime cut-point assert; hash-guarded per-example `24dac078…`; 3-way revision identity `d2b342c6…`; no new materialization. Bit-exact triangulation pre-reg/script/manifest/calibration: τ_lo=0.02458901317356486 (=`p95_null_noise`), τ_hi=0.06829080323934116 (=`meaningful_P5_AUC["1.5"]`, σ_m=1.5), seed `0xd37ec7`, dataset `a29f8f2c…`. τ_lo>0, τ_lo<τ_hi strict. Band origin = separate calibration, not a pos-control noise arm → no Arm-4 circularity.
- **Charge 3 (no-peeking) — PASS (one residual hazard):** real run never a smoke (non-canonical seed/reps WARN + still real); per-class marginal = disclosed mechanism; only HDG-beyond-confidence sealed. Disclosed marginal verified consistent to the unit: 652·0.773 + 1348·0.632 ≈ 1356 correct → 644 errors (=calibration `n_errors`), 652+1348=2000, gap 0.141. **Residual hazard (not a violation):** every invocation computes + (default `--out`) writes real `D`; seal rests on operator discipline + WARN. A lock-tag self-guard would harden it.
- **Charge 4 (§0.5 coherence) — PASS:** mechanism justifies candidate; `pred` over signed margin consistent (avoids slope/intercept re-coupling); three interpretations parity; anti-rescue clause load-bearing; foil well-formed (in-place shuffle, shared split, independent stream `0xC1A55D^0xF011`, non-gating, expected <τ_lo); caveat sound (attaches to interpretation only, never `verdict_of`).
- **Charge 5 (output §7 + reporting) — PASS for §7** (all fields, nothing beyond; stdout not written to file). **REPORTING ERROR FOUND (the #1–#3 failure mode):** §0.5.5 prose said foil "preserves the 65/35 marginal"; actual `pred` marginal is 1348:652 = 67.4:32.6 ≈ **67/33** (exact counts in the data are correct & persisted; only prose rounding wrong). **→ FIXED:** §0.5.5 now reads "67/33 marginal (1348:652)" before it could propagate into the result doc.
- Reporting discipline correctly observed: rung claim properly bounded (#4 does not advance §6; no community-validated over-claim).
- **Cold-pass 1 verdict: conformant; clear to lock once the 65/35→67/33 correction is made (DONE).** Optional hardening recommended: lock-tag guard in the judge so the sealed run cannot be triggered pre-lock.

### Divergences/nits log (§7)
- D1 (reporting): "65/35" → "67/33 (1348:652)" in §0.5.5. **Resolved** (prose only; no gating/computed value touched).
- H1 (hazard, open design-decision): no-peeking seal currently rests on operator discipline + WARN; lock-tag self-guard would convert it to a structural guard. **Routed to Earl** (retrofit #4 vs fold into v0.2 judge-template for all future candidates).

## Cold pass 2 — COMPLETE (genuinely cold reader; no design history; on the 67/33-corrected lock-candidate bytes). NO BLOCKING NON-CONFORMANCE.
Reader verified only the disclosed marginal (652·0.773 + 1348·0.632 → 644 errors = calibration `n_errors`; overall acc 0.678) and **declined to execute the judge** so as not to compute the sealed `D`. Correct discipline.
- **Charge 1 (v0.1 gating) — CONFORMS:** §1 HDG form, AUC higher-is-better (τ_lo sign guarantee holds); §2 all five incl. mechanistic Ancestry; §6 partition strict `>τ_hi`/`<τ_lo`, both boundaries → Inconclusive (exclusive+exhaustive verified); `D=median` all 200 no-trim, byte-identical band; only `D` gates (AP + foil non-gating, no second statistic enters §1).
- **Charge 2 (inheritance) — CONFORMS:** runtime cut-point assert; per-example sha matches calibration's `detector_per_example_sha256`; manifest 3-way revision identity + redundant cut-point identity vs script source AND calibration JSON; no tokenizer/model/inference path.
- **Charge 3 (no-peeking) — CONFORMS (procedural seal, honestly labeled):** non-canonical seed/reps print "STILL computes ... NOT a no-peeking smoke"; smoke is the separate synthetic harness (`B,y` only); per-class marginal = disclosed context, HDG-beyond-confidence computed only by the locked run. **Recorded observation (not a defect):** seal is procedural, not mechanical — nothing in the judge prevents a pre-lock invocation revealing `D`; protection is the synthetic smoke harness + single-execution lock ordering. (= H1; already logged as a v0.2 design seed.)
- **Charge 4 (§0.5 coherence) — COHERENT, two notes:** mechanism justifies candidate (counts/accuracies reproduce `n_errors=644`); three interpretations parity; anti-rescue clause sound; genuine uncertainty real and sealed. Notes CP2-N1, CP2-N2 below.
- **Charge 5 (output §7 + reporting) — §7 CONFORMS; reporting partially dischargeable:** full persistence contract present incl. all three symmetric foil band fractions (the symmetry itself is the structural guard against selective foil reporting); stdout not written to file. Result-doc recurrence risks to avoid (out-of-band, for the write-up): (a) presenting the disclosed 14-pt marginal as the gain/verdict basis rather than the sealed HDG; (b) treating raw `D` as cross-row comparable (§5 forbids — only verdicts compare, conditional on pinned baseline/metric/estimator); (c) reporting `foil_clears_tau_lo` without the §6 caveat. Symmetric persistence guards (c); (a)/(b) are prose discipline for `RESULT_…`.

### Divergences/nits log (§7) — cold pass 2
- **CP2-N1 (foil caveat attribution — imprecise; FOLD candidate, pending Earl):** §6 caveat attributes a foil clearing `τ_lo` to "the class-imbalance marginal doing nontrivial work." But `pred_perm` is **target-independent** (permutation destroys `pred↔correctness`, preserves only the 67/33 frequency), and a target-independent feature **cannot lift held-out AUC in expectation** — so a material `D_foil ≥ τ_lo` is a **methodological red flag** (permutation/independence bug, leakage, estimator/band pathology), NOT legitimate marginal predictiveness. Conservative in effect (only ever attaches a warning, never moves the gate), classified by the reader as a wording/mechanism nit, not a soundness failure. **STATUS: PENDING fold decision** (locked pre-committed interpretation → fold candidate under discipline-addition #1).
- **CP2-N2 (target-fit taxonomy out-of-band — no action):** §0.5.6's Class-B/Class-C taxonomy is a v0.2 Descriptor-Justification-Layer construct, not in the v0.1 body, so a pure cold reader can't discharge it from the artifact alone. **DISCHARGED by existing labeling** — §0/§0.5 already declare the Layer a v0.2 seed, additive and gating-neutral; observation recorded, no change needed.
- **Cold-pass 2 verdict: no blocking non-conformance; clear to lock** (modulo the CP2-N1 fold decision).

---

## === FOLD CP2-N1 (Option 3, full two-pass re-route) — REVIEW LADDER RESET (2026-06-10) ===
**Decision (Earl):** Option 3, full re-route. CP2-N1 is a **substantive** fold, not editorial — it shifts a pre-committed causal story from "class-imbalance marginal earning its keep" (benign) to "methodological red flag — investigate before interpreting" (an alarm). Those are different claims; locking the wrong one as pre-committed interpretation is the exact failure mode pre-commitment exists to prevent. The codified bar (build-and-smoke → warm → two cold) applies as written; clear-modulo was explicitly rejected to avoid eroding the "the locked artifact is exactly the one that was read" bar set at the truncation NB1 fold.
**Fold applied:** §6 foil pre-commitment (line 91) reworded — foil reframed as a **band-validity / methodology check**; a material `D_foil ≥ τ_lo` is a methodological red flag (target-independent feature cannot lift held-out AUC), not legitimate marginal structure; a cleared foil **suspends interpretive trust** pending investigation; gating verdict unaffected (foil non-gating).
**Scope:** §6 caveat prose only. No code, no gating rule, no pinned constant, no seed touched. `judge_predclass.py` / `build_manifest_detector_predclass.py` byte-identical.
**Ripple sites for the fresh cold passes to check (named, not pre-judged):** §0.5.5 construction description; §4 foil build; §7 foil-boolean semantics (`foil_pre_commitment_met` / `foil_clears_tau_lo`); downstream result-doc interpretation. Fix-author assessment (non-clearing): all three read neutral/compatible (construction-only, deferring interpretation to §6) — but the independent read is what discharges this.
**Prior Warm / Cold-1 / Cold-2 above:** SUPERSEDED for the §6-caveat content (they reviewed the pre-fold attribution). Their findings on all other sections stand as historical record; the binding review is the fresh ladder below.
**Build-and-smoke:** STANDS — no code/constants/seed change, so the synthetic smoke (incl. foil mechanic) is unaffected; `py_compile` unaffected. Re-smoke is a no-op confirmation, not a new gate.

## Warm pass (post-fold) — COMPLETE (Earl), CLEAR for cold routing
- §6 reworded paragraph reads correctly on both axes: foil's epistemic role is now "band-validity / methodology check" (not an imbalance-marginal probe), grounded in `pred_perm` being target-independent (cannot lift held-out AUC in expectation, so the band must reject it); the pre-committed alarm (cleared foil → methodological red flag → suspend interpretive trust; failure modes named: permutation/independence bug, leakage, estimator/band pathology) is correctly attached, gating verdict unaffected.
- Three ripple sites checked, all compatible with the new framing: §0.5.5 (construction-only, defers to §6; 67/33 in place); §4 (build-and-persist mechanics, no causal claim); §7 (foil booleans — `foil_pre_commitment_met = D_foil<TAU_LO`, `foil_clears_tau_lo = D_foil>=TAU_LO` — values correct under either framing; only their *meaning* refined by §6, to the more accurate one).
- **PROVENANCE CORRECTION (recorded for byte-exact accuracy):** the three symmetric foil fractions were NOT added alongside the §6 fold — they landed at the FIRST warm-pass fold, *before* CP1. **Both CP1 and CP2 reviewed the three-fraction script** (each explicitly confirmed all three present). `judge_predclass.py` is therefore **byte-identical to what CP1/CP2 reviewed**; the CP2-N1 fold edited only §6 pre-reg prose. The sole delta between the CP2-reviewed bytes and the current bytes is the §6 caveat rewording. No post-cold-pass script drift.
- Warm-pass verdict: CLEAR for two fresh cold passes on the current bytes. Warm (context-saturated), not cold.

## Cold pass 1 (post-fold) — COMPLETE (genuinely cold reader; seal intact — read only disclosed marginal + inherited constants, did NOT compute `D`). CONDITIONAL CLEAR → four fixes applied.
Both re-route questions answered: **(a) §6 reframing sound — YES** (the chain holds: global permutation makes `pred_perm` target-independent; under a linear additive estimator a target-independent main effect cannot raise out-of-sample AUC in expectation — no interaction term for the preserved marginal to act through — so `E[D_foil] ≤ 0`; quantitative anchor: null-noise mean ΔAUC ≈ 0.0024 and `τ_lo = 0.0246` is that arm's P95, so a clean foil median sits ~10× below `τ_lo`, and clearing it would require the whole null to shift ~10× → "artifact, not structure" is the only band-consistent reading; red-flag threshold is well-calibrated. "Gating verdict unaffected" confirmed in code — `verdict_of(D)` reads the real median only; foil lives in `support_nongating`; trust-suspension ≠ second verdict statistic, so §1 one-measure-per-version preserved). **(b) ripple sites consistent — mostly, one stale site (finding #1).**
Standing conformance all PASS: gating §1/§2/§6 (single measure, three-region partition deterministic in `D`, matches `verdict_of`); inheritance (runtime `verify_cut_points`, per-example sha guard, 3-way revision identity, no materialization; band byte-matches calibration, external pre-lock provenance); no-peeking (real HDG every run, refuses to be the smoke, separate B/y-only harness, marginal disclosed/HDG sealed); §0.5 coherence (mechanism→candidate clean, parity interpretations, anti-rescue clause, item-3 honesty that the verdict does not follow from the 14.1-pt gap — flagged the strongest part of the draft); §7 (all fields persisted, modulo #3). Disclosed-marginal reconciled seal-safe: 0.773−0.632=0.141; 652+1348=2000; count-weighted acc 0.678 → ~644 errors = calibration `n_errors`.

### Findings → resolutions
- **#1 (stale terminology ripple — script comments; the load-bearing catch). FIXED.** `judge_predclass.py` docstring and the `foil_clears_tau_lo` inline comment still said "§6 imbalance caveat" — the pre-reframing interpretation the reworded §6 explicitly rejects, read precisely where a result-doc author looks. Renamed both to the **RED-FLAG caveat** (methodological artifact, NOT legitimate marginal structure; suspend interpretive trust). Comment/docstring only — executable logic byte-identical (the logic CP1 verified is untouched). *This is the ripple a targeted pass would have missed — Option 3 earned its cost.*
- **#2 ("materially" asserted in prose, not implemented). FIXED.** §6 said "materially clears `τ_lo`" while `foil_clears_tau_lo = (D_foil ≥ τ_lo)` is a bare crossing (no margin). Chose the no-new-constant fix: dropped "materially," tied the prose to the exact boolean — "clears `τ_lo` (i.e. `D_foil ≥ τ_lo`, the persisted `foil_clears_tau_lo` condition)." Bare crossing is conservative (flags readily = safe direction); prose now matches computed.
- **#3 (`foil_verdict` persisted but not in §7's enumeration vs "nothing beyond"). FIXED.** Added `foil_verdict` (= `verdict_of(D_foil)`, captures the `> τ_hi` case) to §7's enumerated outputs. Pre-reg prose only.
- **#4 (§2 criterion-2 nesting — strengthening, not a defect). APPLIED.** Made the nesting justification explicit: HDG compares nested models `[B] ⊂ [B, pred]`, so the baseline is by construction never more complex than the candidate model — airtight rather than feature-count shorthand. Pre-reg prose only.
- **Cold-pass 1 (post-fold) verdict: clear with #1 fixed (done); #2–#3 housekeeping (done); #4 strengthening (done).** Seal intact; foil reframing sound.

### Re-route disposition (path taken)
Findings #1–#4 are **prescribed consistency/prose completions of the already-decided CP2-N1 reframing** — none introduces a new semantic decision (#1 propagates §6's meaning into comments; #2 aligns prose to an existing boolean; #3 documents an already-persisted field; #4 restates an existing structural fact). By the established practice of this study (the original warm-1 → CP1 → 65/33 fix → CP2 ladder was **not** restarted for prescribed prose fixes; only the substantive CP2-N1 semantic shift triggered a fresh full re-route), these belong to **this** re-route's cold cycle, not a new one. Executable script logic is byte-identical and received CP1's cold read; CP2 (post-fold) reads the final bytes (renamed comments + the three prose fixes). **Disposition: continue the current cycle — route CP2 (post-fold) on the corrected bytes; do not restart the ladder.** (Build-and-smoke STANDS: no executable/constant/seed change.)

## Cold pass 2 (post-fold) — COMPLETE (genuinely cold reader; spec/artifacts only; seal honored — real HDG never computed). CLEARABLE; no blocking non-conformance.
Disclosed marginal recomputed seal-safe (67.4/32.6, 14.1-pt gap, implied 644 errors = calibration `n_errors`). Five charges PASS. Both re-route questions: **(a) foil reframing sound** — sharpened: `τ_lo` literally *is* `p95(null_noise)` from the calibration, so the permuted-pred foil is a fresh noise draw and its median sitting below that p95 bar is near-tautological — the foil validates the band/method, not `pred`; never enters `verdict_of`, so it touches interpretive trust (v0.2), not the v0.1 gate (not a prohibited second statistic). **(b) ripple sites consistent** — §0.5.5 / §4 build / §7 booleans match line-for-line; the two foil booleans are exact complements; boundary handling agrees with §6's `≥ τ_lo`. Findings #1–#4 from CP1(post-fold) confirmed correctly applied. Findings doc: `…local_aba56eea…/outputs/CROSSPASS_DETECTOR_PREDCLASS_OVP_coldpass.md`.

### CP2(post-fold) notes — NON-BLOCKING; disposition PENDING Earl
- **D-1 (divergence-grade; "act before lock"):** a cleared foil leaves a recorded verdict the result-doc is forbidden to interpret, but that obligation is only *implicit*. Reader proposes a locked result-doc banner keyed on `foil_clears_tau_lo == true`. (Note: §6 already locks "suspends interpretive trust pending investigation" — the obligation exists and is enforceable at the result-doc cold pass; D-1 asks to make the result-doc banner an *explicit pre-commitment*.)
- **T-1 (minor):** state the nesting→simplicity reading explicitly. (Largely covered by finding #4's §2 edit; candidate for a one-word tighten or no-op.)
- **T-2 (minor; strengthening, file-set change):** include the smoke harness in the locked set so no-peeking is auditable from the locked record. (Program-level no-peeking-auditability discipline — candidate for a v0.2 template seed parallel to H1, vs a #4-only file-set retrofit.)
- **T-3 (minor):** clarify the foil is a non-gating diagnostic, not a §3 setup-control. (One-line pre-reg clarification.)
- **O-1 (out-of-band):** POSCONTROL_v1 pass is taken on assertion (not re-verified). Not about this artifact; note only.
- **Cold-pass 2 (post-fold) verdict: CLEARABLE; two-cold bar met on reviewed bytes; D-1 + tightenings non-blocking, disposition pending.**

## === DISPOSITIONS BOUND — LOCK-READY (Option A, 2026-06-10) ===
Both post-fold cold passes cleared with no blocking non-conformance → the codified two-cold bar is met on the locked bytes. The CP2(post-fold) notes are dispositioned to their correct levels; none reopens the substantive cycle (none changes gating, constants, computation, or a pre-committed interpretation — D-1 strengthens the *operational form* of an obligation already locked in §6, which is exactly the narrow-editorial case the v0.2 taxonomy is meant to license, not the strict "substantive fold → full re-route" default).

- **D-1 — BOUND HERE (binding, not intent):** **Pre-committed result-doc requirement:** if the locked run produces `foil_clears_tau_lo == true`, `RESULT_DETECTOR_PREDCLASS_OVP.md` MUST carry a prominent banner stating the run is methodologically suspect pending investigation, per §6's locked obligation. **The result-doc cold-pass brief explicitly verifies this banner is present and correctly worded when `foil_clears_tau_lo == true`.** (Substantive obligation: §6, locked. Operational mechanism: enforced at the result-doc cold-pass boundary — the artifact that would carry or omit the banner. Both binding; no pre-reg byte change.)
- **T-2 — v0.2 design seed (confirmed):** lock the smoke harness so no-peeking is auditable from the locked record — program-level, applied uniformly across all judge scripts at the v0.2 template, NOT a #4-only file-set retrofit (same asymmetry logic as H1). Recorded in `OVP_DESIGN_HISTORY.md` alongside H1 and the re-route-taxonomy seed.
- **T-1, T-3 — v0.x editorial queue (no fold):** T-1 (nesting→simplicity reading) largely covered by §3 / finding-#4's §2 edit; absorbed by v0.2 template work. T-3 (foil is non-gating diagnostic, not a §3 setup-control) already derivable from §4/§6's non-gating framing. Neither blocks; neither folded into the about-to-lock artifact.
- **O-1 — cross-pass-brief-template note:** "POSCONTROL_v1 taken on assertion" is a property of the cold-reader package, not an artifact defect; future cold-pass packages should include the `ovp-poscontrol-v1-result` lock notice + signed tag as external attestation. Recorded for the brief template, not a here-and-now action.

**LOCK-READY:** three named files frozen (`PRE_REGISTRATION_DETECTOR_PREDCLASS_OVP.md`, `judge_predclass.py`, `materialization_manifest_detector_predclass.json`); build-and-smoke stands; warm + two cold (post-fold) cleared; dispositions bound above.

## === LOCKED + RUN COMPLETE — VERDICT #4 (2026-06-10) ===
**Lock:** commit `d4964f0`, signed tag `detector-predclass-ovp-lock` (3 files, gates green: dataset/model/cut-point/per-example all identity-checked). **Single execution**, seed `0xC1A55D`, R=200; outputs → `detector_predclass_results.json` (to be tagged `detector-predclass-ovp-result`).
- **`pred` → INCONCLUSIVE.** `D = 0.028142692244269796` ∈ `[τ_lo, τ_hi]`; +0.00355 above `τ_lo`, 0/200 reach `τ_hi`, 43.5% below `τ_lo`. Borderline; no legitimacy claim (§0.5.4).
- **Foil (non-gating): band VALIDATED.** `D_foil = −0.00508`, **200/200 below `τ_lo`** (`foil_frac_below_tau_lo = 1.0`), `foil_pre_commitment_met = True`, **`foil_clears_tau_lo = False`**.
- **D-1 RESOLVED:** `foil_clears_tau_lo == False` → the suspend-interpretation banner is **correctly absent**; run is methodologically trustworthy. (The result-doc cold-pass brief still verifies this: confirm no banner, because the foil did not clear.)
- **Substantive finding:** the intercept-shaped candidate #3 speculated "would plausibly Validate" returns Inconclusive — folded confidence already largely encodes the 14.1-pt class-asymmetry; item-3's tempering vindicated; an over-optimistic pre-registered forecast caught by the locked run.
- Ledger: Inconclusive / Not-Validated / Inconclusive / Inconclusive — four real verdicts, zero false positives.
- **Result doc drafted:** `RESULT_DETECTOR_PREDCLASS_OVP.md`. **NEXT GATE: its own cold cross-pass (citation gate, spec §7)** before citable — brief includes the D-1 no-banner check.

## === RESULT-DOC COLD PASS — CLEAR; CITATION GATE SATISFIED (2026-06-10) ===
Genuinely cold reader recomputed **all 16 gating + support quantities directly from the `hdg_distribution` arrays** (numpy `'linear'`, R=200) and checked each against both the stored JSON field and the prose: **all match to machine precision (diff = 0.00e+00).** No seal to honor (run complete, `D` committed) — pure fidelity/conformance/framing audit.
- **(1) Numerical fidelity:** clean — D, margin, P5/median/mean/P95, three band fractions, AP median, full foil block, per-class diagnostic, n/errors all trace.
- **(2) Verdict conformance:** `band_relation` recomputes (in_band=True); Inconclusive read per §6; §0.5.4 honored (abstention, "no legitimacy claim," "borderline"/"just above the noise floor," "does not establish that pred adds nothing") — no near-miss/trend spin.
- **(3) D-1:** `foil_clears_tau_lo == False` confirmed; banner correctly absent and affirmatively reported as satisfied (neither omitted-when-due nor invented-when-not).
- **(4) Recurrence risks:** (a) 14.1-pt marginal only as disclosed mechanism/context, separated from verdict basis; (b) raw `D` not cross-row-comparable — restricted to verdict-level "both #3 & #4 borderline Inconclusive"; (c) foil reported with §6 band-validity/RED-FLAG framing. All held.
- **(5) Fair framing + provenance:** "#3's 'would plausibly Validate' not borne out" stated as Inconclusive ≠ Validated (honest non-confirmation, not overstated to a refutation); §5 ledger fields correct; seed/R/foil-stream/hashes/estimator/band byte-match manifest+JSON+calibration.
- **Verdict: CLEAR — no discrepancies, no required corrections. Result doc faithful to JSON, conformant to locked spec. Citation gate (§7) satisfied; `RESULT_DETECTOR_PREDCLASS_OVP.md` is citable.**

**DETECTOR_PREDCLASS_OVP COMPLETE:** locked (`detector-predclass-ovp-lock`) → run (`detector-predclass-ovp-result`) → result cold-passed (citable). Remaining: records commit (result doc + this CROSSPASS + 3 v0.2 seeds) + ledger row.

---

### Lock checklist (after passes clear)
- [ ] `build_manifest_detector_predclass.py` run; all four gates green (inherited per-example hash; dataset hash; 3-way model-revision identity; cut-point identity vs frozen calibration).
- [ ] Atomic commit stages ONLY: `PRE_REGISTRATION_DETECTOR_PREDCLASS_OVP.md`, `judge_predclass.py`, `materialization_manifest_detector_predclass.json` (NEVER `git add .`).
- [ ] Signed tag `detector-predclass-ovp-lock`.
- [ ] `OVP_DESIGN_HISTORY.md`: mark first prospective application of the Descriptor Justification Layer (dual-interpretation lock + surface foil operative).
- [ ] Run once (no flags) under seed `0xC1A55D`; persist `detector_predclass_results.json`.
- [ ] Write `RESULT_DETECTOR_PREDCLASS_OVP.md`; route its result cold-pass (citation gate); record the fourth ledger row.
