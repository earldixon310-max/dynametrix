# Cross-Pass Record — DETECTOR_DIRECTION_OVP (candidate study, OVP real candidate #3)

Tracks the two-pass review + lock progress for `PRE_REGISTRATION_DETECTOR_DIRECTION_OVP.md`, per spec §7. Produces OVP's **third** real candidate verdict — **reaching the operational rung** (§6.2). Both pass verdicts carried into the ledger row.

**Lock bar:** two independent passes return **no lock-blocker on byte-identical artifacts**; a blocker resets the count; non-blockers queue without folding; the fix-author cannot clear.

## Current status: **EXECUTED — verdict INCONCLUSIVE; OVP OPERATIONAL.** Locked at `detector-direction-ovp-lock` (2/2 clean cold passes, corrected ancestry); single run under seed `0xDEC0DE`; outputs at `detector-direction-ovp-result`. **`D = 0.026446 ∈ [0.024589, 0.068291]` → Inconclusive** (borderline; +0.0019 above τ_lo, 44.5% of reps below). **OVP real verdict #3 → OPERATIONAL rung reached** (§6.2). Non-gating diagnostic: 14-point intercept-level reliability asymmetry (acc 0.773 pred=AI vs 0.632 pred=Human) — **vindicating the §3/§9 fold** (the unfolded "symmetric null in both" claim would have been contradicted). Outcome doc: `RESULT_DETECTOR_DIRECTION_OVP.md`. **Open gate: cold cross-pass on the result document** (§7 citation gate). v0.x non-blockers remain queued.

## Revision-2 cold pass 2 (external, genuine fresh reader): NO LOCK-BLOCKER — BAR MET
Independent fresh reader (routed with the **corrected** prompt) re-derived the non-nesting algebra (slope-asymmetry `2w_p` and intercept-asymmetry `w_p` governed by the same coefficient → a pure-intercept shift forces `w_p=0`; `[B,pred]` non-nested) and confirmed **both overclaim risks correctly avoided** — §3 states "not strictly more powerful than pred," §9 scopes the null to "no slope-type asymmetry on the axis `[B,p]` spans." All gating checks conform (candidate/baseline, V-shape full-rank spot-checked, all-inherited hash-verified, band verbatim + `verify_cut_points`, paired splits no leakage, D=median, no-peeking, output/§7, rung = operational not community-validated). **VERDICT: NO LOCK-BLOCKER.** Two non-blockers (queued un-folded): smoke harness not in packet (confirmed at lock via build-findings); `support_nongating` field accrual (all enumerated in §7; cosmetic).

## Revision-2 cold pass 1 (external, genuine fresh reader): NO LOCK-BLOCKER (1 of 2 on corrected bytes)
Fresh reader re-derived the slope/intercept algebra and confirmed the corrected §3/§9 is statistically sound (non-nested; neither dominates; the pure-intercept asymmetry `[B,pred]` catches is missed by `[B,p]`, and vice versa). All gating checks conform (candidate/baseline, all-inherited hash-verified materialization, band verbatim + `verify_cut_points`, paired splits no leakage, D=median, no-peeking post-verdict diagnostics, output/§7, rung accounting). **VERDICT: NO LOCK-BLOCKER.** Counts as 1 of 2 (the reviewer reviewed the byte-identical corrected artifact and cleared it on correct grounds).
- **Process finding (IMPORTANT, fixed):** the cold-reader **prompt memo** I routed still demanded confirmation of the *old false* claim ("strictly more powerful / stronger symmetric null") — a stale derived artifact not re-synced when §3/§9 were folded. The reviewer correctly reviewed the *artifact* (not the memo), confirmed the corrected framing, and flagged the memo as inverted. **Prompt corrected before routing pass 2.** Lesson: the review prompt is derived from the pre-reg's framing; folding a finding requires re-syncing the prompt too, or the next independent reader is misdirected. (Queued for the post-lock design-history note.)
- Other non-blockers (queued un-folded): `n_examples`/`n_errors` nested under `support_nongating` vs §7's top-level listing (cosmetic, same as #2); `verify_cut_points` exact-float-equality (sound, exact round-trip); CSV-hash/smoke not byte-verifiable from inline copies (scope; closed by the real-file packet + manifest guard).

## Revision 2 — §3/§9 "strictly more powerful" overstatement corrected (2026-06-08); bytes changed, count reset
Cold pass 2 (below) returned **NO LOCK-BLOCKER** but flagged a **weighted, non-gating** finding: §3 called raw `p` **"strictly more powerful"** than `pred`, and §9 claimed a non-positive verdict supports a symmetric null in **both slope and intercept** — both **mathematically false** under the linear estimator. `[B,p]` and `[B,pred]` are **non-nested**: `[B,p]` captures class-specific *slope* (coupled intercept), `[B,pred]` captures a *pure-intercept* shift that `[B,p]` cannot represent. Neither dominates. The reviewer (correctly) classified it NO-LOCK-BLOCKER (it is in §3 rationale + §9 non-gating hooks, not the gating path / band / verdict / §2 Ancestry) and recommended queuing for the result-doc gate. **Operator chose to FOLD now, not queue** — on the principle that OVP's core value is *the locked record is trustworthy*, so knowingly locking a false mathematical claim (even non-gating) is a crack in the strongest part of the structure, worth one reset to avoid. §3 reworded to "non-nested / neither strictly dominates / raw `p` tests the slope-asymmetry axis (not strictly more powerful than pred)"; §9 scoped the null to "no slope-type asymmetry on the axis `[B,p]` spans; does not rule out pure-intercept asymmetry." Script unchanged (verdict logic untouched → synthetic smoke + check F still hold; no re-smoke). Count resets to zero.
**[design-history, post-lock]** Worth recording: a non-gating finding folded *because* it would lock a false claim into the permanent pre-reg — a named principle ("fold even non-gating findings that would commit a false statement to the locked record").

## Cold pass 2 (external, genuine fresh reader) — SUPERSEDED by revision 2 (operator folded Finding 1): NO LOCK-BLOCKER
Independent fresh reader; re-derived the slope/intercept algebra and confirmed all gating checks (candidate/baseline, all-inherited hash-verified materialization, band verbatim + `verify_cut_points`, paired splits no leakage, D=median, no-peeking post-verdict diagnostics, output/pin/scope, rung accounting). **VERDICT: NO LOCK-BLOCKER.** Weighted Finding 1: the "strictly more powerful" / "stronger symmetric null in both slope+intercept" overstatement (non-gating; §3/§9) — folded into revision 2 (above). Finding 2 (scope): the synthetic smoke harness was not in the reviewer's packet — documented in build-findings; confirm at lock.

## Cold pass 1 (external, genuine fresh reader) — SUPERSEDED by revision 2: NO LOCK-BLOCKER

## Cold pass 1 (external, genuine fresh reader): NO LOCK-BLOCKER
Fresh reader (spec + locked calibration result + inherited per-example + pre-reg + script). **Numerically re-derived the powerful-test ancestry algebra:** `p = pred·B + (1−pred)(1−B)` exact; `[B,p]` fits slope `(w_B+w_p)` for pred=1 / `(w_B−w_p)` for pred=0 (class-specific slope + coupled intercept) vs `[B,pred]`'s shared-slope/intercept-only; lower residual for `[B,p]` (0.087 vs 0.108) → strictly more powerful, stronger symmetric null on a non-positive; rank 3 full-rank (V-shape, both classes populated). Spot-checked `B=max(p,1−p)` and `pred=1{p≥0.5}` (0 violations). All-inherited materialization + hash guard, no-peeking (verdict computed before any directional marginal; per-class diagnostics post-verdict, non-gating), D=median no trimming, cut points verbatim + `verify_cut_points`, paired splits no leakage, output/pin/scope conformance — all confirmed. **VERDICT: NO LOCK-BLOCKER.**
Three non-blockers (queued un-folded): (1) §8 synthetic smoke harness not in the reviewer's packet — confirm at lock (documented in build-findings; it ran). (2) couldn't recompute the CSV sha in their env — guards verified in code; pass 2 (with the real CSV) closes it. (3) §3's "slope plus intercept" is *accurate* but could add a one-clause note that the intercept is **coupled** (one free `w_p`), not two independent intercepts — editorial.

## Pre-cold-pass steps (complete)
- **Build-and-smoke** (`judge_direction.py`, `SCRIPT_BUILD_FINDINGS_DETECTOR_DIRECTION_OVP.md`): hardening present from draft 1. **No peeking (heightened)** — smoke synthetic-only (B,y); `predicted_prob_ai`/`pred` never loaded and the directional marginal not computed: known-null → Not-Validated, known-meaningful → Validated. ✓
- **Output-conformance (check F):** every §7 output produced incl. the non-gating per-predicted-class diagnostics; nothing unpinned.
- **Manifest generator** (`build_manifest_detector_direction.py`): aborts on inherited per-example-hash, dataset-hash, 3-way model-revision-identity, or cut-point-identity mismatch — all verified PASS vs the real files.
- **Operator local `py_compile`** of `judge_direction.py`: **PASS (2026-06-08)**.

## Warm conformance audit (operator, full design context — NOT a §7 cold pass): NO BLOCKERS
Adversarial A–I walk; all conform — measure form (§4 = spec §1); **(B)** all five §2 criteria incl. the **labeled Ancestry Statement correct on draft 1** (the corrected powerful-not-equivalent / class-specific-slope framing — no Ancestry reset cycle, unlike truncation #1); verdict open/open/closed; **(D)** `D=median` over all 200 no trimming, verdict reads D only; **(E)** cut points verbatim + `verify_cut_points` runtime assert (NB2 hardening baked in); **(F)** paired splits + train-fit standardization no leakage; **(G, heightened no-peeking)** docstring states the honest framing (NB1 footgun corrected proactively), `judge()` never sees `pred`, per-class diagnostics computed post-verdict in the locked run only, smoke synthetic-only; **(H)** output conformance incl. the non-gating per-class diagnostics; **(I)** scope (correctly frames the operational-rung transition). V-shape full-rank check confirmed. **VERDICT: NO BLOCKERS.**
- **Non-blocker (cold-reader fact-check, not a defect):** §0's framing cites verdict #1 Inconclusive / #2 Not-Validated; cold readers should verify against `RESULT_DETECTOR_TRUNCATION_OVP.md` and `RESULT_DETECTOR_LENGTH_OVP.md` (both committed/citable) — give them those docs in the packet. The claims are accurate; the verdict derivation does not depend on them.
- **Observation (queue for post-lock design-history note):** the candidate-#1 hardening (NB1 docstring no-peeking, NB2 runtime cut-point assert, hash-guard pattern) and the Ancestry Statement landed in **draft 1**, not folded mid-review — yesterday's worked-example discipline now applied prospectively, no reset cycle. Worth naming in `OVP_DESIGN_HISTORY.md` after lock.

## Remaining gates (revision-2 corrected bytes)
- ~~Two cold passes~~ **DONE: 2 of 2 clean on byte-identical corrected artifacts — BAR MET.**
- **Operator §11 sign-off** on the 7 discretionary pins — the one open gate before lock.
- Lock: `build_manifest_detector_direction.py` (4 gates) → atomic commit of pre-reg + `judge_direction.py` + manifest → signed tag `detector-direction-ovp-lock` → **single run** (inherit + judge, no materialization) → commit outputs + `detector-direction-ovp-result` → `RESULT_DETECTOR_DIRECTION_OVP.md` + result cross-pass → **third ledger row → OVP operational.**

### Post-lock design-history notes (two principles earned this cycle)
1. **Fold even a non-gating finding that would commit a false statement to the locked record** — the locked record's trustworthiness is OVP's core value; a false claim is a crack in the strongest part of the structure (the §3/§9 overstatement fold).
2. **Derived review instructions must be re-synced when the artifact they reference is folded** — a stale cold-reader prompt that still demanded the old false claim was caught by an independent reader; the review *process* has its own artifacts that can drift.

---
## (historical) revision-1 gates
- **Warm pass** (pinned pre-commitments: candidate raw `predicted_prob_ai`; the corrected powerful-not-equivalent Ancestry Statement / class-specific-slope point; inherited band verbatim; D=median; paired splits; no-peeking; per-class diagnostics non-gating), then **two independent cold passes** (≥1 cold, fix-author cannot clear).
- Operator §11 sign-off.
- Lock (`detector-direction-ovp-lock`: pre-reg + `judge_direction.py` + manifest, atomic commit + signed tag) → **single run** (inherit + judge, no materialization) → verdict → `RESULT_DETECTOR_DIRECTION_OVP.md` + result cross-pass → **third OVP ledger row → operational rung reached.**

## Note
Verdict sealed: `predicted_prob_ai`'s HDG and the directional marginal have not been computed pre-lock (smoke synthetic-only). This is the candidate most likely to land Validated (if the detector's reliability is class-asymmetric) — the first Validated would be a notable first, so the no-peeking discipline is held especially tightly here.

## === RESULT-DOC COLD PASS — CLEAR; CITATION GATE SATISFIED (2026-06-10) ===
Genuinely cold reader recomputed **every figure in `RESULT_DETECTOR_DIRECTION_OVP.md` from the `detector_direction_results.json` 200-element arrays** (not the prose): D, mean, min/P5/P95/max, the +0.00186 margin, 0/200 above `τ_hi`, 44.5%/55.5% split, 97% positive, AP median +0.0042, per-class diagnostic (504+852=1356 correct → 644 errors, gap +0.141). **All match exactly — zero drift.** No seal to honor (run complete, `D` committed).
- **(1) Numerical fidelity:** clean, every figure traces.
- **(2) Verdict conformance:** `D ∈ band` → Inconclusive read per §6; abstention with parity; 97%-positive immediately counterweighted by borderline framing — no spin; operational rung correctly claimed, community-validated correctly declined.
- **(3) Keystone (ancestry scoping):** HOLDS — "no strong slope-type asymmetry on the axis `[B,p]` spans," explicitly does not rule out the (large) pure-intercept asymmetry; never reverts to "strictly more powerful" / "symmetric null in both"; correction parenthetical accurate, not gloating.
- **(4) Forward speculation:** "[B,pred] would plausibly Validate" properly hedged, framed as a distinct question, disclaimed in "Does not"; fair on #3's evidence alone.
- **(5) Fair framing + provenance:** intercept-level framing accurate and bounded; "misses AI" logically sound; seed `0xDEC0DE`, R=200, model/dataset/per-example hashes, `τ` provenance tag, estimator all match across JSON/calibration/judge.
- **Verdict: CLEAR — citable. Two non-blocking nits, both folded post-clear (precision/completeness, fix-author-applied, cold-reader-prescribed; no re-route):**
  - **N1 (precision):** "the correction was borne out by the data" → "the data are consistent with the correction, directly refuting the intercept-symmetry half (the slope scoping rests on the estimator argument, not this run)." The data refute the intercept half; they do not confirm the slope null.
  - **N2 (§5 completeness):** ledger-row table now includes `operator` and `date` fields; citation-gate line marked CLEARED 2026-06-10.

**DETECTOR_DIRECTION_OVP COMPLETE:** locked → run → result cold-passed (citable). Last open citation gate on the detector arc now closed; all four detector candidate verdicts (#1–#4) fully recorded and citable.
